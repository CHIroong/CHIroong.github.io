	var ref;

function Querystring(qs) {
	this.params = {};
	if (qs == null) qs = location.search.substring(1, location.search.length);
	if (qs.length == 0) return;

	qs = qs.replace(/\+/g, ' ');
	var args = qs.split('&');
	for(var i=0; i<args.length; i++)
	{
		var pair = args[i].split('=');
		var name = decodeURIComponent(pair[0]);
		var value = (pair.length==2) ? decodeURIComponent(pair[1]) : name;
		this.params[name] = value;
	}

	this.get = function(key, default_)
	{
		var value = this.params[key];
		return (value != null) ? value : default_;
	}
}

try {
	var qsMobile = new Querystring();
	var rqMobile = qsMobile.get("ref");

	if (typeof(rqMobile) != "undefined" && rqMobile == "false") {
		ref=true;
	}
} catch(e){}
 
var isIPhone = !ref && navigator.userAgent.indexOf("iPhone")>=0;
var isIPad = !ref && navigator.userAgent.indexOf("iPad")>=0;
var isWinCE = !ref && navigator.userAgent.indexOf("Windows CE")>=0;
var isMobile = !ref && navigator.userAgent.indexOf("Mobile")>=0;
var isAndroid1 = !ref && navigator.userAgent.indexOf("Android 1")>=0;
var isAndroid2 = !ref && navigator.userAgent.indexOf("Android 2")>=0;

if(isIPad == true){}
else if(isIPhone || isWinCE || isMobile || isAndroid1 || isAndroid2 == true){
	var idxurl = window.location.href;

    if( idxurl.indexOf("cboard") > -1) { 
		var url = idxurl.replace("cboard", "mboard");
		document.location.href = url;
	}
}
